# 🍲 Recipe Site

Хоолны жоруудыг хялбар байдлаар үзэх, хайх, хадгалах зориулалттай веб сайт.

## 🚀 Эхлүүлэх заавар

### Шаардлагатай зүйлс
- **Node.js** (v14 эсвэл түүнээс дээш)
- **MySQL** (v8.0 эсвэл түүнээс дээш)
- **Python** (v3.x - frontend серверт зориулсан)

### 1️⃣ Татаж авах

```bash
git clone https://github.com/Badamjav369/Recipe-Site.git
cd Recipe-Site
```

### 2️⃣ Өгөгдлийн сан тохируулах

1. MySQL-д нэвтэрч, өгөгдлийн сан үүсгэх:
```bash
mysql -u root -p
```

2. Schema файл ажиллуулах:
```sql
source database/schema.sql
```

Эсвэл:
```bash
mysql -u root -p < database/schema.sql
```

### 3️⃣ Backend тохируулах

1. Backend folder руу орох:
```bash
cd backend
```

2. Package суулгах:
```bash
npm install
```

3. `.env` файлыг засах (өөрийн MySQL мэдээллээр):
```env
DB_HOST=localhost
DB_USER=root
DB_PASSWORD="StronP#45"
DB_NAME=RecipeSite
DB_PORT=3306
PORT=3000
JWT_SECRET=secret123
```

4. Жоруудыг өгөгдлийн санд нэмэх (анх удаа):
```bash
node migrate-recipes.js
```

### 4️⃣ Серверүүдийг эхлүүлэх

#### Backend сервер (Terminal 1):
```bash
cd backend
node server.js
```
✅ Backend: http://localhost:3000

#### Frontend сервер

**Live Server (VS Code extension) - Санал болгож байна**
1. VS Code-д index.html файлыг нээх
2. Баруун товчоор "Open with Live Server" сонгох
3. Автоматаар http://127.0.0.1:5500 эсвэл http://localhost:5500 дээр нээгдэнэ


✅ Frontend: http://localhost:8000

### 5️⃣ Сайтыг нээх

Live Server ашигласан бол: **http://127.0.0.1:5500** эсвэл **http://localhost:5500**


---

## 👤 Тест хэрэглэгч

Сайтад нэвтрэхэд дараах мэдээллийг ашиглана уу:

- **И-мэйл:** badamjav@gmail.com
- **Нууц үг:** 123457

Эсвэл:

- **И-мэйл:** yalguun@gmail.com
- **Нууц үг:** 123456

---

## 🛠️ Технологи

### Frontend
- HTML5
- CSS3 (Custom Properties)
- JavaScript (ES6 Modules)
- Web Components

### Backend
- Node.js
- Express.js
- MySQL
- JWT Authentication
- bcrypt

---

## 📁 Файлын бүтэц

```
Recipe-Site/
├── backend/              # Backend server
│   ├── server.js        # Express сервер
│   ├── db.js            # Өгөгдлийн санд холбох
│   ├── .env             # Тохиргооны файл
│   └── migrate-recipes.js # Жор нэмэх script
├── components/          # Web Components
│   ├── foods-card/      # Хоолны карт
│   ├── login/           # НэвтрэхModal
│   ├── profile/         # Профайл
│   └── recipe-info/     # Жорын дэлгэрэнгүй
├── data/                # JSON өгөгдөл (fallback)
├── database/            # Өгөгдлийн санд схема
├── images/              # Зургууд
└── index.html           # Үндсэн HTML

```

---

## 🔧 Асуудал гарвал

### Сервер холбогдохгүй байвал:
1. Backend сервер ажиллаж байгаа эсэхийг шалгах:
   ```bash
   netstat -ano | findstr :3000
   ```

2. MySQL сервер ажиллаж байгаа эсэхийг шалгах

3. `.env` файл дахь тохиргоо зөв эсэхийг шалгах

### "Cannot find module" алдаа гарвал:
```bash
cd backend
npm install
```

### Өгөгдлийн сан холбогдохгүй байвал:
- MySQL сервер ажиллаж байгаа эсэхийг шалгах
- `.env` файл дахь DB_PASSWORD зөв эсэхийг шалгах
- Өгөгдлийн сан үүссэн эсэхийг шалгах:
  ```sql
  SHOW DATABASES;
  USE RecipeSite;
  SHOW TABLES;
  ```

---

## 📝 Онцлог

✅ Жор үзэх, хайх, шүүх  
✅ Хэрэглэгч бүртгэл, нэвтрэх  
✅ Жор хадгалах  
✅ Жор үнэлэх  
✅ Жор нэмэх (хэрэглэгчээр)  
✅ Профайл хуудас  
✅ Монгол хэл дэмжлэг
